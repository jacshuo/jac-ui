export { Dialog } from "./Dialog";
export { Tooltip } from "./Tooltip";

import { Dialog } from "./Dialog";
import { Tooltip } from "./Tooltip";

export const Overlay = { Dialog, Tooltip };

export default Overlay;
